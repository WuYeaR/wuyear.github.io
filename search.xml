<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Linux 配置</title>
    <url>/2023/02/26/linux%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<hr>
<h3 id="安装zsh包"><a href="#安装zsh包" class="headerlink" title="安装zsh包"></a>安装zsh包</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt install zsh</span><br></pre></td></tr></table></figure>

<h3 id="安装Oh-My-Zsh框架"><a href="#安装Oh-My-Zsh框架" class="headerlink" title="安装Oh My Zsh框架"></a>安装Oh My Zsh框架</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sh -c <span class="string">&quot;<span class="subst">$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)</span>&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="推荐插件"><a href="#推荐插件" class="headerlink" title="推荐插件"></a>推荐插件</h3><h4 id="Powerlevel10k"><a href="#Powerlevel10k" class="headerlink" title="Powerlevel10k"></a><em>Powerlevel10k</em></h4><pre><code><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --depth=1 https://github.com/romkatv/powerlevel10k.git <span class="variable">$&#123;ZSH_CUSTOM:-<span class="variable">$HOME</span>/.oh-my-zsh/custom&#125;</span>/themes/powerlevel10k</span><br><span class="line"><span class="built_in">exec</span> zsh</span><br><span class="line">p10k configure</span><br></pre></td></tr></table></figure>
</code></pre>
<h4 id="zsh-syntax-highlighting"><a href="#zsh-syntax-highlighting" class="headerlink" title="zsh-syntax-highlighting"></a><em>zsh-syntax-highlighting</em></h4><pre><code> <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --depth=1 https://github.com/zsh-users/zsh-syntax-highlighting.git <span class="variable">$&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;</span>/plugins/zsh-syntax-highlighting</span><br></pre></td></tr></table></figure>
</code></pre>
<h4 id="zsh-autosuggestions"><a href="#zsh-autosuggestions" class="headerlink" title="zsh-autosuggestions"></a><em>zsh-autosuggestions</em></h4>   <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git clone --depth=1 https://github.com/zsh-users/zsh-autosuggestions $&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;/plugins/zsh-autosuggestions</span><br></pre></td></tr></table></figure>

<h4 id="z-lua"><a href="#z-lua" class="headerlink" title="z.lua"></a><em>z.lua</em></h4>   <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --depth=1 https://github.com/skywind3000/z.lua.git <span class="variable">$&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;</span>/plugins/z.lua</span><br></pre></td></tr></table></figure>

<h4 id="autoupdate-zsh-plugin"><a href="#autoupdate-zsh-plugin" class="headerlink" title="autoupdate-zsh-plugin"></a><em>autoupdate-zsh-plugin</em></h4>   <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> --depth=1 https://github.com/TamCore/autoupdate-oh-my-zsh-plugins <span class="variable">$ZSH_CUSTOM</span>/plugins/autoupdate</span><br></pre></td></tr></table></figure>
<h3 id="zshrc配置"><a href="#zshrc配置" class="headerlink" title=".zshrc配置"></a>.zshrc配置</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ZSH_THEME=<span class="string">&quot;powerlevel10k/powerlevel10k&quot;</span></span><br><span class="line">plugins=(sudo git z.lua extract command-not-found autoupdate zsh-autosuggestions zsh-syntax-highlighting)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>配置</category>
        <category>美化</category>
      </categories>
      <tags>
        <tag>美化</tag>
        <tag>zsh</tag>
        <tag>Linux配置</tag>
      </tags>
  </entry>
  <entry>
    <title>条款3-尽可能使用const</title>
    <url>/2023/03/05/%E6%9D%A1%E6%AC%BE3-%E5%B0%BD%E5%8F%AF%E8%83%BD%E4%BD%BF%E7%94%A8const/</url>
    <content><![CDATA[<h1 id="条款3-尽可能使用const"><a href="#条款3-尽可能使用const" class="headerlink" title="条款3-尽可能使用const"></a>条款3-尽可能使用const</h1><h2 id="const简介"><a href="#const简介" class="headerlink" title="const简介"></a>const简介</h2><p><code>const</code>是一个语义约束(指定一个<strong>不该被改动</strong>的对象), 而编译器会强制实施这项约束, </p>
<ol>
<li><code>const</code>可以在<code>class</code>外, 修饰<code>global</code>或者<code>namespace</code>中被声明为static的对象</li>
<li>可以用来修饰<code>class</code>内部的<code>static</code>和<code>non-static</code>成员变量</li>
<li>面对指针, 你可以指出指针的本身, 指针所指物, 或者两者都(或都不)是<code>const</code></li>
</ol>
<h2 id="const在指针中的应用"><a href="#const在指针中的应用" class="headerlink" title="const在指针中的应用"></a>const在指针中的应用</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> greeting[] = <span class="string">&quot;Hello&quot;</span>;       <span class="comment">// non-const pointer, non-const data</span></span><br><span class="line"><span class="type">char</span>* p = greeting;              <span class="comment">// non-const pointer, non-const data</span></span><br><span class="line"><span class="type">const</span> <span class="type">char</span>* p = greeting;        <span class="comment">// non-const pointer, const data</span></span><br><span class="line"><span class="type">char</span>* <span class="type">const</span> p = greeting;        <span class="comment">// const pointer, non-const pointer</span></span><br><span class="line"><span class="type">const</span> <span class="type">char</span>* <span class="type">const</span> p = greeting;  <span class="comment">// const pointer, const data</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>const在星号的左边, 表示被指物是常量, 如果出现在右边, 表示指针自身是常量, 如果出现在星号两边, 表示被指物和指针本身都是常量</p>
</blockquote>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 两种的位置都是可以的</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f1</span><span class="params">(<span class="type">const</span> Widget* pw)</span></span>;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f2</span><span class="params">(Widge <span class="type">const</span>* pw)</span></span>;</span><br></pre></td></tr></table></figure>

<p>STL的迭代器是以指针为根据设计出来, 所以迭代器的作用就像个<code>T* 指针</code>, 声明迭代器为<code>const</code>就像声明指针为<code>const</code>一样, 即声明一个<code>T* const指针</code>, 表示迭代器<strong>不得指向不同的东西</strong>, 但是它所指的值是可以改动的, 如果你希望迭代器所指的东西不可改动, 需要使用<code>const_iterator</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; vec&#123;<span class="number">1</span>&#125;;</span><br><span class="line">std::vector&lt;<span class="type">int</span>&gt;::iterator iter = vec.<span class="built_in">begin</span>();</span><br><span class="line">*iter = <span class="number">10</span>;    <span class="comment">// 可以改变iter所指物</span></span><br><span class="line">++iter;        <span class="comment">// 错误 不可以改变iter</span></span><br><span class="line">std::vector&lt;<span class="type">int</span>&gt;::const_iterator cIter = vec.<span class="built_in">begin</span>();</span><br><span class="line">*cIter = <span class="number">10</span>;   <span class="comment">// 错误 *cIter是const</span></span><br><span class="line">++cIter;       <span class="comment">// 可以改变cIter</span></span><br></pre></td></tr></table></figure>

<h2 id="const在函数声明中的应用"><a href="#const在函数声明中的应用" class="headerlink" title="const在函数声明中的应用"></a>const在函数声明中的应用</h2><div class="note danger"><p><em><strong><del>本小节已经过时</del></strong></em><br>在<code>C++ 11</code>之后已经不推荐<br><code>C++ 11</code>引入了左值和右值, 返回一个<code>non-const value</code>能够充分利用右值, 比如: <code>(a + b).expensive();</code></p>
</div>

<details>
<summary>Effective C++关于const返回的介绍</summary>
返回一个常量值, 往往可以降低客户错误而造成的意外, 又不至于放弃安全性和高效性, 如:

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Ratianal</span> &#123;&#125;</span><br><span class="line"><span class="type">const</span> Rational <span class="keyword">operator</span>* (<span class="type">const</span> Rational&amp; lhs, <span class="type">const</span> Rational&amp; rhs);</span><br><span class="line">Rational a, b, c;</span><br><span class="line">(a * b) = c;    <span class="comment">// 在a * b的结果上调用operator=</span></span><br></pre></td></tr></table></figure>

<p>比如如果客户写的笔误时:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (a * b = c) &#123;  <span class="comment">// 其实是想做一个==操作</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>能和内置类型一样, 直截了当的显示不合法</p>
</details>

<h2 id="const在成员函数中的应用"><a href="#const在成员函数中的应用" class="headerlink" title="const在成员函数中的应用"></a>const在成员函数中的应用</h2><p><code>const</code>成员函数的目的是让该成员函数可以作用于<code>const</code>对象</p>
]]></content>
      <categories>
        <category>Effective C++笔记</category>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>C++</tag>
        <tag>Effective C++</tag>
      </tags>
  </entry>
  <entry>
    <title>条款1: 视C++为一个联邦</title>
    <url>/2023/02/27/EffectiveC-%E7%AC%94%E8%AE%B0-2023-02-27-%E6%9D%A1%E6%AC%BE1-%E8%A7%86C-%E4%B8%BA%E4%B8%80%E4%B8%AA%E8%81%94%E9%82%A6/</url>
    <content><![CDATA[<h1 id="视C-为一个联邦"><a href="#视C-为一个联邦" class="headerlink" title="视C++为一个联邦"></a>视C++为一个联邦</h1><hr>
<h2 id="1-C与C"><a href="#1-C与C" class="headerlink" title="1. C与C++"></a>1. C与C++</h2><ol>
<li>C以C++为基础</li>
<li>Object-Oriented C++</li>
<li>templates</li>
<li>STL</li>
</ol>
<h2 id="2-C-是一门多重泛型的语言"><a href="#2-C-是一门多重泛型的语言" class="headerlink" title="2. C++是一门多重泛型的语言"></a>2. C++是一门多重泛型的语言</h2><ol>
<li>过程形式(procedural)</li>
<li>面向对象形式(object-oriented)</li>
<li>泛型形式(generic)</li>
<li>元编程形式(metaprogramming)</li>
<li>函数形式(functional)</li>
</ol>
<h2 id="C-是一门语言的联邦而不是一个单独的语言-他是四个次级语言组成的-联邦-每个次语言都有自己的规约"><a href="#C-是一门语言的联邦而不是一个单独的语言-他是四个次级语言组成的-联邦-每个次语言都有自己的规约" class="headerlink" title="C++是一门语言的联邦而不是一个单独的语言, 他是四个次级语言组成的___联邦___, 每个次语言都有自己的规约"></a>C++是一门语言的联邦而不是一个单独的语言, 他是四个次级语言组成的___联邦___, 每个次语言都有自己的规约</h2><p>比如在<code>内置类型C like</code>而言, pass-by-value通常更高效, 但是到了Object-Oriented, 由于构造和析构的存在, pass-by-reference-const往往更好, template C++更是如此, 因为在展开前, 你不知道所处理的对象的类型, <strong>但是</strong>, 到了STL迭代器和数组而言, pass-by-value再次适用<br>所以, 每个次语言都有自己的规约, 不要惊讶</p>
]]></content>
      <categories>
        <category>Effective C++笔记</category>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>C++</tag>
        <tag>Effective C++</tag>
      </tags>
  </entry>
  <entry>
    <title>条款2: 替换#define</title>
    <url>/2023/02/27/EffectiveC-%E7%AC%94%E8%AE%B0-2023-02-27-%E6%9D%A1%E6%AC%BE2-%E6%9B%BF%E6%8D%A2-define/</url>
    <content><![CDATA[<h1 id="尽量以const-enum-inline替换-define"><a href="#尽量以const-enum-inline替换-define" class="headerlink" title="尽量以const, enum, inline替换#define"></a>尽量以const, enum, inline替换#define</h1><hr>
<h2 id="为什么要替换-define"><a href="#为什么要替换-define" class="headerlink" title="为什么要替换#define"></a>为什么要替换#define</h2><p>假设有存在一个定义</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> PI_M 3.1415</span></span><br></pre></td></tr></table></figure>

<ol>
<li>M<code>在编译前就被预处理器移走了, 所以从未进入过</code>symbol table<code>, 如果在使用变量但是出现编译期错误信息时, 提到的是</code>3.1415<code>而不是</code>PI_M&#96;</li>
<li>同理在<code>symbol debugger</code>中也可能因为没有进入<code>symbol table</code>而为了追踪它浪费时间</li>
<li>使用<code>#define</code>可能会因为<em>预处理器盲目的将<code>PI_M</code>替换而导致<code>3.1415</code>在<code>object code</code>中出现多次</em></li>
</ol>
<h2 id="用常量替换宏"><a href="#用常量替换宏" class="headerlink" title="用常量替换宏"></a>用常量替换宏</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> PI_M 3.1415</span></span><br><span class="line"><span class="comment">// 使用常量来代替宏(#define)</span></span><br><span class="line"><span class="type">const</span> <span class="type">double</span> PI_M = <span class="number">3.1415</span>;</span><br></pre></td></tr></table></figure>
<p>对于指针的使用需要注意*******前后的const</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">char</span>* authorName = <span class="string">&quot;JJWu&quot;</span>;        <span class="comment">// 指向一个字符常量, 不可以改变字符的内容</span></span><br><span class="line"><span class="type">char</span>* <span class="type">const</span> authorName = <span class="string">&quot;JJWu&quot;</span>;        <span class="comment">// 常量指针, 不可以指向其他字符</span></span><br><span class="line"><span class="type">const</span> <span class="type">char</span>* <span class="type">const</span> authorName = <span class="string">&quot;JJWu&quot;</span>;  <span class="comment">// 指向常量字符的常量指针</span></span><br></pre></td></tr></table></figure>


<div class="note info no-icon"><p><em><strong>注意:</strong></em></p>
<p><em>C++98中规定, 在<code>命名空间 namespace</code>中的const是隐式static的</em></p>
</div>



<details>
<summary>关于static与extern的对比</summary>

<blockquote>
<p><em><strong><em>Translation unit:</em> A source file after the pre-processor (recursively) included all its include files.</strong></em><br><em><strong><em>static linkage:</em> A symbol is only available within its translation unit.</strong></em><br><em><strong><em>ernal linkage:</em> A symbol is available from other translation units.</strong></em></p>
</blockquote>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">const</span> <span class="type">int</span> sci = <span class="number">0</span>; <span class="comment">// sci is explicitly static</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> ci = <span class="number">1</span>;         <span class="comment">// ci is implicitly static</span></span><br><span class="line"><span class="keyword">extern</span> <span class="type">const</span> <span class="type">int</span> eci = <span class="number">2</span>; <span class="comment">// eci is explicitly extern</span></span><br><span class="line"><span class="keyword">extern</span> <span class="type">int</span> ei = <span class="number">3</span>;        <span class="comment">// ei is explicitly extern</span></span><br><span class="line"><span class="type">int</span> i = <span class="number">4</span>;                <span class="comment">// i is implicitly extern</span></span><br><span class="line"><span class="type">static</span> <span class="type">int</span> si = <span class="number">5</span>;        <span class="comment">// si is explicitly static</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

</details>

<h2 id="在类中用enum替换宏"><a href="#在类中用enum替换宏" class="headerlink" title="在类中用enum替换宏"></a>在类中用enum替换宏</h2><p>为了将常量的<strong>作用域scope</strong>限制于class中, 你必须让它成为<code>class</code>的一个成员<code>member</code>, 为了确保这个常量只有一份实体, 必须要成为一个<code>static</code>的成员:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GamePlayer</span>&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">static</span> <span class="type">const</span> <span class="type">int</span> numTurns = <span class="number">4</span>;  <span class="comment">// 常量声明式</span></span><br><span class="line">    <span class="type">int</span> scores[numTurns];           <span class="comment">// 使用该常量</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">printf</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        cout &lt;&lt; &amp;numTurns &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p><code>numTurns</code>是一个<em><strong>声明式</strong></em>, <em>通常来说C++要求你对你所使用的东西提供一个_定义式_</em>, 但是如果变量是一个<code>class成员</code>, 且是<code>static</code>且类型是整数类型(如<code>int char bool</code>), 我们可以声明并使用而不用提供定义式</p>
<p>但是, 当我们尝试获取它的地址时, 我们必须提供他的定义式:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> GamePlaer::numTurns;<span class="comment">// 因为在声明时将它的初值设为4, 所以不用提供初值</span></span><br></pre></td></tr></table></figure>

<p>类的专属变量无法使用<code>#define</code>的方式来定义, 因为<code>#define</code>不重视<strong>scope作用域</strong>, 一旦宏被定义, 在后续的使用中都是有效的, 除非被<code>#undef</code></p>
<p>如果旧版本的编译期不支持上述的做法<del>static成员在其声明式上获得初值</del>, 此外所谓的<code>in-class 初值设定</code>也只允许对整数常量进行</p>
<p>如果不支持, 则只能通过如下方式进行:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">CostEstimate</span> &#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">static</span> <span class="type">const</span> <span class="type">double</span> FudgeFactor;<span class="comment">// static class常量声明</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">double</span> CostEstimate::FudgeFactor = <span class="number">1.35</span>;<span class="comment">//static class常量定义</span></span><br></pre></td></tr></table></figure>

<p>如果需要在<code>编译期</code>提供一个<code>class常量值</code>, 例如<code>GamePlayer::scores</code>的数组声明式, 如果编译期不支持<del><em><strong>static整数型class常量完成in class初值设定</strong></em></del>, 可以使用<em>the enum hack</em>的补偿方法</p>
<p><em>the enum hack</em>比较像<code>#define</code>而不是<code>const</code>, 例如取一个<code>const</code>的地址是合法的, 但是取一个<code>enum</code>和<code>#define</code>的地址是不合法, 另外不够聪明的编译器, 也可能给<code>const</code>变量分配额外的内存, <code>enum</code>和<code>#define</code>绝对不会</p>
<p><em>enum hack</em>是很实用的, <em>enum hack</em>是<code>template metaprogramming</code>的基础技术</p>
<blockquote>
<p><strong>如果你不想让别人获得一个<code>pointer</code>或者<code>reference</code>指向你的某个整数常量, enum可以帮助你实现这个约束</strong></p>
</blockquote>
<h2 id="替换宏函数"><a href="#替换宏函数" class="headerlink" title="替换宏函数"></a>替换宏函数</h2><p>#define另外一个常用的实现宏<code>macros</code>, 宏看起来像是函数, 但是不会导致函数调用带来额外的开销</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> CALL_WITH_MAX(a, b) f((a) &gt; (b) ? (a) : (b))</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 缺点, 调用f之前, a累加的次数竟然取决于&quot;a被拿来和谁比较&quot;</span></span><br><span class="line"><span class="type">int</span> a = <span class="number">5</span>, b = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">CALL_WITH_MAX</span>(++a, b);      <span class="comment">// a被累加2次</span></span><br><span class="line"><span class="built_in">CALL_WITH_MAX</span>(++a, b + <span class="number">10</span>); <span class="comment">// a被累加1次</span></span><br></pre></td></tr></table></figure>

<p>另外一个麻烦的是必须要为宏参数中的所有实参加上小括号, 否则可能会出现问题</p>
<blockquote>
<p>解决宏的方法就是<strong>template inline 函数</strong></p>
</blockquote>
<p><strong>template inline 函数</strong>具有宏的效率以及一般函数的所有可预料行为和类型安全性<code>type safety</code></p>
<p>一个<strong>template inline 函数</strong>如下所示:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">callWithMax</span><span class="params">(<span class="type">const</span> T&amp; a, <span class="type">const</span> T&amp;b)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">f</span>(a &gt; b ? a : b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>具备优点:</p>
<blockquote>
<p>不需要在函数本体中为参数加上括号</p>
<p>不需要操心参数被核算(求值)多次</p>
<p>遵守作用域<code>scope</code>和访问规则, 例如可以写一个<strong>class 类的private inline函数</strong></p>
</blockquote>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><blockquote>
<p><em><strong>对于单纯常量, 最好以<code>const</code>对象或者<code>enum</code>来代替<code>#define</code></strong></em></p>
<p><em><strong>对于形似函数的宏, 最好改用<code>inline函数</code>来替代<code>#define</code></strong></em></p>
</blockquote>
]]></content>
      <categories>
        <category>Effective C++笔记</category>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>C++</tag>
        <tag>Effective C++</tag>
      </tags>
  </entry>
</search>
